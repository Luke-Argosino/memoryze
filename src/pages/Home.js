import { useHistory } from "react-router-dom";

const Home = (props) => {
    return (
        <div className="Home">
            A spaced repitition flash card web app.
        </div>
    )
}

export default Home;